
sury_apt_key: 'https://packages.sury.org/php/apt.gpg'

sury_apt_repositories:
 - 'deb https://packages.sury.org/php/ {{ ansible_distribution_release }} main'

sury_apt_packages:
  - apt-transport-https